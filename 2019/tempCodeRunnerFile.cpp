str = regex_replace(str, regex(" *( *I *)|( *me *) *"), " you ");
        // str = regex_replace(str, regex(" *can you *"), "I can ");
        // str = regex_replace(str, regex(" *could you *"), "I could ");
        // str = regex_replace(str, regex("\\?"), "!");